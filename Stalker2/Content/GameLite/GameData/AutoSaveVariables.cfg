DefaultConfig : struct.begin
   // Time interval between auto saves (in seconds). Has to be > 0 for timed autosaves to work.
   AutoSaveIntervalTime = 600
   AutoSaveOnFailRetryIntervalTime = 10
struct.end
